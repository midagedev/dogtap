# G1 Evidence Output

Capture scripts write local run artifacts here, usually under `latest/`.

This directory is ignored except for this README and `.gitignore` because raw
fixture evidence can contain sensitive test payloads. Review generated artifacts
before promoting sanitized examples into `fixtures/`.

